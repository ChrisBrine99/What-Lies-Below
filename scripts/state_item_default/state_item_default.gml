/// @description The default state for the inventory's item section. From here the user will be able to move
/// through their inventory slots and select the highlighted item, which will open up the inventory's item
/// command menu.

// Borrow the parent menu's menument script
menu_movement();